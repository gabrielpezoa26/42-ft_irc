atualizado em 15/04/2026

testes auto pra rodar de vez em quando p ter ctz q nd quebrou rs
	-> roda make run
	-> abre outro terminal, e roda um arquivo .sh (./test.sh   por exemplo)





exemplo de teste manual:
	-> roda make run em um terminal, esse vai ser o terminal do servidor
	-> abre outro terminal e roda make nc, esse vai ser o terminal do client
	-> faz a mesma coisa em outro terminal
	-> entao vai ficar 1 terminal pro servidor, e 2 pra clients
	-> fzr o login com senha, nick e user nos 2 clients
		pass <senha>
		nick <seu_nick>
		user <seu_user> 0 * <:seu_nome_real>
	-> PRIVMSG <nick> <mensagem>
	-> "dividir" a mensagem com ctrl + D, testa lógica do tcp
	-> no outro client, é pra aparecer a mensagem recebida 


exemplo de comando USER:
	USER gcesar-n 0 * :Gabriel
	
	gcesar-n         username
	0                "modo", o 0 é meio q o padrão
	*                o nome do server, tbm é meio que padrão
	:Gabriel         o nome real da pessoa



testar com o Irssi:

- sudo apt install irssi      (deve vir a versao 1.4.x ou similar)

- abre o irssi e roda:
	- /connect -nocap 127.0.0.1 <port> <password>
			obs: na versao das maquinas da 42 mto provavelmente n
			precisa do '-nocap'

- usa "normal" pra enviar msgs etc (aí tem q ver na documentação deles rsrs)